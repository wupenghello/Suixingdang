{
  "name": "suixingdang-web",
  "private": true,
  "type": "module",
  "description": "随行档前端工具链：vitest 单元测试（生产仍零构建，仅开发/CI 用）",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "dompurify": "^3.1.6",
    "jsdom": "^24.1.0",
    "marked": "^12.0.2",
    "vitest": "^1.6.0"
  }
}
