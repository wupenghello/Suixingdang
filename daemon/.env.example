 # 守护进程配置
 # 复制为 .env 并填写
 
 # 你的服务器地址（带 https://）
 SERVER_URL=https://files.yourdomain.com
 
 # 设备令牌（在 Web 设置页面创建）
 DAEMON_TOKEN=在这里填入你创建的令牌
 
 # 要同步的本地文件夹
 WATCH_DIR=~/suixingdang-sync
 
 # 同步模式: two_way（双向）或 upload_only（仅上传）
 SYNC_MODE=two_way
 
 # 全量比对轮询间隔（秒）
 POLL_INTERVAL=300
